# LinknLink Remote 环境变量配置示例
# 复制此文件为 .env 并填写实际值

# =============================================================================
# 必需配置
# =============================================================================


# =============================================================================
# 可选配置
# =============================================================================

# 日志级别: trace, debug, info, notice, warning, error, fatal
# 默认: info
LOG_LEVEL=info

# Haddons API 地址
# 默认指向宿主机 8099 端口
# Bridge 模式下请使用 host.docker.internal
# HADDONS_API_BASE_URL=http://host.docker.internal:8099

# 代理目标 IP
# Docker 运行 (Bridge): 默认为 host.docker.internal (docker-compose 已自动配置，无需修改)
# 本地运行 (Host): 默认为 127.0.0.1
# TARGET_IP=127.0.0.1

# 可配置项目前仅保留日志级别
